train_lae_pgmult <- function(X, Y, X_new, s, r, K=NULL, sigma=1e-3, cl=NULL,
                             models=list(subsample="kmeans",
                                         gl="rw",
                                         root=FALSE)) {

}
